Bootstrap: localimage
From: base.sif

%post
  micromamba install -y -q -c conda-forge -c pytorch -c https://software.repos.intel.com/python/conda faiss-cpu=1.14.0 libsvs-runtime=0.1.0
  micromamba clean --all --yes
